# Minecraft Local Server GUI (Electron App)

This folder contains the Electron + React (Vite) frontend.

For the full project documentation, see the repository root `README.md`.

## Development

```bash
npm install
npm run dev
```

## Build Installer (Windows)

```bash
npm run electron:build
```

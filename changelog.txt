# Minecraft Server GUI - Changelog

## Version 1.2.6 - 2026-06-02 READLINE / STARTUP SCRIPTS FIX

### FIXED:
- **Startup scripts (run.sh/run.bat) no longer executed** — The app now uses direct JAR-based startup instead of running `run.sh`/`run.bat` scripts. Fixes `undefined symbol: rl_macro_display_hook` crash on Gentoo Linux / AppImage environments (Issue #11)

---

## Version 1.2.5 - 2026-05-28 DNS PROXY, FIXED ADDRESSES & CONSOLE

### CRITICAL FIX:
- **Fixed backend crash on startup (.exe)** — Added missing hidden imports (`requests`, `pydantic`, `starlette`, `multipart`) to PyInstaller build
- **Error logging** — Backend crash logs are now saved to `%APPDATA%/MinecraftServerGUI/backend_crash.log`

### ERROR FEEDBACK (NEW):
- **Server error detection** — Common errors (missing mods, Java version, port conflicts, OOM) are detected and shown as structured error banners with fix suggestions
- **Mod dependency warnings** — When installing a mod that requires other mods, shows a confirmation dialog
- **Banner in Dashboard** — Clear, actionable error messages with dismiss button

### NEW: Fixed Server Addresses (DNS)
- **Permanent domain** — Each server gets a permanent address like `survival.play.tudominio.app` that never changes
- **Auto-generated** — Unique subdomain generated automatically from server name + ID (`vanilla-a3f2b`)
- **Inline editing** — Click the pencil icon to change your subdomain, with availability checking
- **Duplicate protection** — Subdomains are permanently reserved; not released when tunnel stops
- **Cloudflare DNS Proxy** — Open-source Cloudflare Worker manages SRV records (repo: MLSG-DNS-Worker)
- **Auto-Tunnel** — Tunnel starts automatically 5s after server boot (toggle in Advanced settings)

### Console Improvements
- **Search & filter** — Search bar and level badges (CMD/INF/WRN/ERR) for quick log navigation
- **Export logs** — Download button saves full console history as `.txt`
- **Polling fallback** — Console fetches logs via REST API when WebSocket is disconnected
- **Fixed WebSocket 404** — `wsproto` dependency added to fix WebSocket in .exe builds

### UI & Dashboard
- **Redesigned tunnel section** — Clean single-address display, DNS always visible, Advanced toggle
- **Local IP** — Always shown as subtitle alongside public/DNS address
- **Online/Offline badges** — Clear DNS status indicator (ONLINE/OFFLINE)
- **Premium/No Premium toggle** — Quick online-mode switch in Dashboard header
- **Boot from library** — "Boot" button on server cards to start server directly from library
- **Mods import** — Upload `.jar` files directly with file picker in Mods tab
- **Auto-restart on crash** — Toggle in Dashboard header, max 3 restart attempts

### Bug Fixes
- Fixed console tab showing "Loading..." forever (input always enabled, REST fallback works)
- Fixed logs disappearing when switching panels (Dashboard & Console stay mounted)
- Fixed Dashboard mini-console unable to send commands without WebSocket
- Various JSX/Babel parsing fixes

---

## Version 1.2.2 - 2026-05-27 CONSOLE HOTFIX

### CRITICAL FIX:
- **Fixed WebSocket 404 in built .exe** — `wsproto` dependency was missing from PyInstaller build, causing `/ws/console` to return 404 and Console tab to receive no logs

### IMPROVEMENTS:
- **Console polling fallback** — Console tab now fetches logs via REST API when WebSocket is disconnected, matching Dashboard behavior
- Added `wsproto` to `requirements.txt` and GitHub Actions PyInstaller builds

---

## Version 1.2.1 - 2026-05-27 CONSOLE & STABILITY UPDATE

### NEW FEATURES:
- **Auto-restart on crash** — Server automatically restarts after unexpected shutdown (toggle in Dashboard header, max 3 attempts)
- **Export console logs** — Download button in Console tab saves logs as .txt file
- **Search & filter in console** — Search bar and level filter (All/CMD/INF/WRN/ERR) for quick log navigation

### BUG FIXES:
- **Fixed console tab loading forever** — Input no longer disabled when WebSocket is disconnected; REST API fallback works now
- **Fixed logs disappearing on tab switch** — Dashboard and Console components stay mounted; log state preserved across panel navigation
- **Fixed Dashboard mini-console input** — REST API fallback added when WebSocket is disconnected
- **Fixed polling fallback** — No longer hardcoded to always use polling; now correctly checks WebSocket connection state

### IMPROVEMENTS:
- Console connection status now shows "Disconnected" (yellow) instead of "Loading..." (gray) when WebSocket is down
- WebSocket errors now logged to console for easier debugging
- Improved input usability in Console tab (always enabled, falls back to REST API)

---

## Version 1.2.0 - 2026-04-30 UI REDESIGN & NEOFORGE

### UI REDESIGN:
- **Pixel art icon system** — Custom 16x16 pixel icons replacing generic icons throughout the entire UI
- **Visual effects** — Mouse spotlight tracking, noise grain overlay, magnetic button hover effects, abstract animated backgrounds
- **Server Library** — Completely redesigned server selector with grid view, search bar, recently opened section, and engine type icons
- **Refined dark theme** — Deeper blacks, improved contrast, smoother glassmorphism panels
- **Error Boundary** — Graceful error handling across the UI with fallback components

### NEOFORGE SUPPORT:
- Full support for **NeoForge** as a server engine
- NeoForge appears in Setup Wizard, Mods browser, and server cards with its own dedicated icon
- Java version mapping correctly handles NeoForge/NeoMinecraft 26.x+ to Java 25

### MODPACKS:
- New **Modpacks** tab in the Mods section — browse and install complete modpacks directly from Modrinth
- Full filtering support: loader, version, category, and sort order

### MULTI-LANGUAGE (i18n):
- Interface available in **English, Spanish, French, and Russian**
- Language selector in the new App Settings panel
- Centralized LanguageContext for easy extension

### APP SETTINGS:
- New dedicated **Settings panel** with language, theme, notifications, auto-start, minimize-to-tray, and update check options

### IMPROVEMENTS:
- **WebSocket Context** — Centralized real-time communication layer
- **Engine icons** — Each server type (Vanilla, Paper, Spigot, Forge, NeoForge, Fabric) has its own visual icon in cards and selectors
- **Improved Mods browser** — Filter by loader (Fabric, Forge, NeoForge, Quilt), version, category, and sort order
- **GitHub Actions** — Added build verification step for both Windows and Linux before publishing releases

---

## Version 1.1.9 - 2026-04-08 LINUX STABILITY & SAFETY UPDATE

### CRITICAL BUG FIXES:
- **Fixed read-only filesystem crash on Linux** — All data goes to `~/.minecraft_server_gui/` on Linux
- **Fixed RAM leak on Linux** — Memory-safe log structures (`deque` instead of `list.pop(0)`)
- **Fixed unkillable app on ALL platforms** — App only stays in background when servers are actively running

### SAFETY IMPROVEMENTS:
- **RAM allocation cap** — Slider limited to 80% of system RAM
- **RAM warning system** — Visual warnings when assigning >50% or >75% of system RAM
- **System info endpoint** — New `/system/info` API returns total RAM

### HOTFIX 1.1.9.1 (Included in v1.1.9):
- Fixed backend restart loop on Windows
- Improved Java version mapping for Java 25
- Fixed Startup Scripts (run.bat/run.sh) environment injection
- Fixed Portable Executable Stability
- Robust Forge version handling

### IMPROVEMENTS:
- Smart close behavior
- Tray icon fixes for Linux
- Parent watchdog fix
- Memory-optimized log storage
- New endpoints — `/servers/running`, `/system/info`
- Multi-server improvements

## Version 1.0.0 - Initial Release
- Initial GUI structure with server start/stop
- Console output and command input
- Server properties viewer and editor
- System resource usage display (CPU/RAM) with graphs
- Player list display
- Ops list display and management
- Worlds list display and backup functionality
- Player statistics viewer
